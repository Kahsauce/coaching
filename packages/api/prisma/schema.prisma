// Prisma schema
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-py"
}

model Session {
  id          Int      @id @default(autoincrement())
  date        DateTime
  sport       String
  durationMin Int
  rpe         Int
}
